# Smart City Traffic Forecasting

Milestone 1 repository foundation.

## Structure

- config/
- src/
- data/
- models/
- outputs/
- notebooks/

Run:

```bash
pip install -r requirements.txt
python main.py
```
Adds validation, feature engineering, statistics and EDA modules.

This milestone introduces a reusable machine learning framework with
base classes, model implementations, evaluation helpers and a training
pipeline skeleton.
